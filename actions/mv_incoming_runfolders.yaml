---
name: 'mv_incoming_runfolders'
runner_type: 'run-local'
description: 'move runfolders from incoming folder to processing folder'
enabled: true
entry_point: ''
parameters:

    cmd:
        immutable: true
        default: 'ssh -v {{ host_user }}@{{ host }} mv {{ incoming folder }} {{ processing_folder }}'
